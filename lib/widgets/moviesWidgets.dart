import 'package:flutter/material.dart';

class MoviesWidgets extends StatelessWidget {
  const MoviesWidgets({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
